# Server (Express + TypeScript)

Quick start:

Install dependencies:

```bash
cd "server"
npm install
```

Development:

```bash
npm run dev
```

This runs the server on `http://localhost:4000` by default. Copy `.env.example` to `.env` to override `PORT`.

Build and start (production):

```bash
npm run build
npm run start
```
